{
  "name": "batenjan",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "gatsby": "^2.21.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "^3.4.1",
    "serve": "^11.3.0"
  },
  "scripts": {
    "deploy": "gatsby build --prefix-paths && gh-pages -d build",
    "start": "gatsby develop",
    "build": "gatsby build --prefix-paths"
  }
}
